** int -> int     #iter |-> pi
** 1 - 1/3 + 1/5 - 1/7 ......
** ⨉× ᕦ(ò_óˇ)ᕤ
** 1 / 1 + (2×i)
** (((Lx / 2) * 2) = Lx)
**
** def pi(i):
**     if i == 0:
**         return 1
**     else:
**         if i % 2 == 0:
**             return 1/(1+(i*2)) + pi(i-1)
**         else:
**             return -1/(1+(i*2)) + pi(i-1)

(@ ($ (Y) 

(λ. λ. (if L0 = 0 then 10000 
                    else (
                        
                        (if ((((L0 / 2) * 2) = L0) ) then (10000 / ((L0 * 2) + 1))
                        else (-10000 / ((L0 * 2) + 1))) + (@ (L1) (L0 + (-1)))
                    )))) (2577)) * 4