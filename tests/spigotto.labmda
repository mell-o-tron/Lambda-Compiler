m@ ($ (Y) (λs. (8)

    if L6 = 0 & ((4 × L5) + L4 + (-L3)) < (L1 × L3) then
        6969

    else if L6 < 0 then (💀) else (
        if ((4 × L5) + L4 + (-L3)) < (L1 × L3) then
            INT (16, (((10 × ((3 × L6) + L5)) / L4) + ((-10) × L2)) + 48, 14, 0,0,0,0,0,0, λ.
                m@(L8) [
                    L7 + (-1),
                    10 × L6,
                    10 × (L5 +- (L2 × L4)),
                    L4,
                    L3, 
                    ((10 × ((3 × L6) + L5)) / L4) + ((-10) × L2),
                    L1
                ]
            )
        else
            m@(L7) [
                L6,
                L5 × L2,
                ((2 × L5) + L4) × L0,
                L3 × L0,
                L2 + 1,
                (L5 × ((7 × L2) + 2) + (L4 × L0)) / (L3 × L0),
                L0 + 2
            ]
            

))) [10, 1,0,1,1,3,3]


** 💀